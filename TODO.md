待办事项：

1. 当没有数据时，在 统计 和 最近记录 页面的内容区中间（上下居中/左右居中）显示提示文本，提示用户当前没有数据。
2. AP 配网时，热点启动过程中应该通过 modal 显示提示，当热点启动成功后，才显示 AP 配网 modal。
3. WIFI 列表应该支持滚动，当 WIFI 列表过长时，用户可以通过滚动查看完整的 WIFI 列表。
4. 连接 WIFI 后，应该参考 设备信息 页面的设计显示网络信息（IP，网关，DNS 等信息）
5. 连接 WIFI 后，应显示二维码，扫描后进入到 web 设置页面。二维码下方应该提示 必须连接到同一 WIFI 才能访问设置页面。
6. 支持在 统计 和 最近记录 页面通过长按确定键录音添加收支记录
